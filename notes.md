# 02/13/26
- Consider multiplrocess on the `ratio_alert.py` to run an instance of the class for each container rather than one instance for all containers. Or implement multiprocess for different functions
- Different data structures for holding many dictionaries and n+size of container queries (n queries for n containers)

## CMS 2019
- "IPAddress": "172.18.0.3"

##
- run dockerself: `sudo -E python3 docker_self.py`
- run commands.py: `sudo python3 commands.py`